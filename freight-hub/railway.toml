[build]
builder = "DOCKERFILE"
dockerfilePath = "freight-hub/Dockerfile"
watchPatterns = ["freight-hub/**", "freight_core/**"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
